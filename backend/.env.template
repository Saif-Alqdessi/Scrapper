# ============================================================
#  LeadForge Backend — Environment Variables Template
#  Copy this file to .env and fill in your actual values.
#  NEVER commit .env to version control.
# ============================================================

# ── Application ─────────────────────────────────────────────
APP_ENV=development            # development | production
APP_SECRET_KEY=leadforge-jwt-secret-9876543210!
API_KEY=MySuperSecretDashboardKey2026
FRONTEND_ORIGIN=http://localhost:3000

# ── PostgreSQL ───────────────────────────────────────────────
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=leadforge
POSTGRES_USER=admin
POSTGRES_PASSWORD=DbPass!2026
# Assembled by config.py — do not set manually unless overriding
# DATABASE_URL=postgresql+asyncpg://admin:password@postgres:5432/leadforge

# ── Redis / Celery ───────────────────────────────────────────
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ── Google Cloud / AI ────────────────────────────────────────
# Places API — for lead scraping
GOOGLE_PLACES_API_KEY=AIzaSyC-codsc8levHzS5dXDfFFdcg5RvAQhmVk

# Gemini API — Copywriters (gemini-1.5-pro) + Analyst/Validator (gemini-1.5-flash)
GOOGLE_AI_API_KEY=AIzaSyBxstlb_olZiMw9ulrtQCn3HgS035wnk9c

# Apify API
APIFY_API_TOKEN=apify_api_...

# ── LangGraph / LangSmith (Optional tracing) ─────────────────
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=leadforge

# ── Preview URL (used in wa.me message generation) ───────────
PREVIEW_BASE_URL=http://localhost:3000/preview

# ── Logging ──────────────────────────────────────────────────
LOG_LEVEL=INFO
