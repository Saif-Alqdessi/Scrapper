# ============================================================
#  LeadForge Backend — Python Dependencies
#  AI stack: Groq (Analyst + Copywriters) + Google AI (Validator)
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-multipart==0.0.17

# ── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0          # Async PostgreSQL driver for SQLAlchemy
alembic==1.14.0
psycopg2-binary==2.9.10  # Sync driver (used by Alembic migrations)

# ── Data Validation ──────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Task Queue ───────────────────────────────────────────────


# ── Google Cloud / AI (Validator: Gemini 2.0 Flash) ──────────────────────
google-generativeai==0.8.3    # Gemini SDK
googleapis-common-protos==1.66.0

# ── Groq (Analyst + Copywriter EN + Copywriter AR — Llama 3.3 70B) ────────────
groq>=0.9.0

# ── Web Scraping ─────────────────────────────────────────────
httpx==0.28.1             # Async HTTP client (Google Places API calls)

# ── AI Orchestration ─────────────────────────────────────────
langgraph==0.2.56
langchain-core==0.3.25
langchain-google-genai==2.0.7   # LangChain integration for Gemini

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
python-slugify==8.0.4     # Slug generation for leads
python-jose[cryptography]==3.3.0  # JWT for API key auth
passlib[bcrypt]==1.7.4
tenacity==9.0.0           # Retry logic for API calls
structlog==24.4.0         # Structured logging

# ── Development / Testing ────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
httpx                     # Already listed above — used for TestClient too
faker==33.1.0
