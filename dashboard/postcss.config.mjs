// postcss.config.mjs — Tailwind v3 PostCSS config
// tailwindcss v3 uses "tailwindcss" plugin, NOT "@tailwindcss/postcss"
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
